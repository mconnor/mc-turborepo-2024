<div style="font-size: 8rem">
  <span class="with-icon">
    <svg class="op-icon icon" viewBox="0 0 200 200">
      <line
        class="op-icon-p"
        x1="66"
        x2="66"
        y1="100"
        y2="175"
        stroke-width="25"></line>
      <circle
        class="op-icon-p"
        cx="100"
        cy="100"
        r="35"
        fill="transparent"
        stroke-width="25"></circle>
      <circle
        class="op-icon-o"
        cx="100"
        cy="100"
        r="85"
        fill="transparent"
        stroke-width="25"></circle>
    </svg>Open Props
  </span>
</div>
<style>
  .icon {
    width: 0.75em;
    width: 1cap;
    height: 0.75em;
    height: 1cap;
  }

  .with-icon {
    display: inline-flex;
    align-items: baseline;
  }

  .with-icon .icon {
    margin-inline-end: 1rem;
  }
  .op-icon-p {
    stroke: var(--indigo-2);
    a:hover & {
      stroke: var(--indigo-1);
    }

    [data-theme='light'] & {
      stroke: var(--indigo-7);
    }

    [data-theme='light'] a:hover & {
      stroke: var(--indigo-8);
    }
  }

  .op-icon-o {
    stroke: var(--indigo-4);
    a:hover & {
      stroke: var(--indigo-3);
    }

    [data-theme='light'] & {
      stroke: var(--indigo-5);
    }
    [data-theme='light'] a:hover & {
      stroke: var(--indigo-6);
    }
  }
</style>
